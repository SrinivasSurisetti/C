#include<stdio.h>
#include<string.h>

int main()
{
    int n;
    char s[1000];
    scanf("%[^\n]s",s);
    n=strlen(s);
    int sum=0,r;
    for(int i=0;i<n;i++)
    {
        if(s[i]>=48&&s[i]<=57)
        {
            r=s[i]-48;
            sum=sum+r;
        }
    }
    printf("%d",sum);
}
